import { Order } from '@/types';

// Categories, MenuItems, and Tables are now fetched from the backend API.
// Only Orders remain as local mock data until backend supports them.

export const defaultOrders: Order[] = [];
